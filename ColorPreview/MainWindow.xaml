<Window KeyUp="MainWindow_OnKeyUp" x:Class="ColorPreview.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ColorPreview"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:vm="clr-namespace:ColorPreview.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <vm:ColorViewModel x:Key="VModel"/>
    </Window.Resources>
    
    <Grid DataContext="{StaticResource VModel}">
     
        <StackPanel>
            <Rectangle Name="Rectangle1" Height="200"/>
            <Slider Name="SliderR" Value="{Binding Color.R}" Maximum="255"  ValueChanged="RangeBase_OnValueChanged" >
            
            </Slider>
            <Slider Name="SliderG" Value="{Binding Color.G}" Maximum="255"  ValueChanged="RangeBase_OnValueChanged"/>
            <Slider Name="SliderB" Value="{Binding Color.B}" Maximum="255" ValueChanged="RangeBase_OnValueChanged"/>
        </StackPanel>
        
    </Grid>
</Window>
